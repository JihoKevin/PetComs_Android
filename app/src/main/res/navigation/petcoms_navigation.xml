<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/petcoms_navigation"
        app:startDestination="@id/recordFragment">
    <fragment
            android:id="@+id/diaryFragment"
            android:name="kevin.android.petcoms.fragments.diary.DiaryFragment"
            android:label="fragment_diary"
            tools:layout="@layout/fragment_diary" />
    <fragment
            android:id="@+id/myPageFragment"
            android:name="kevin.android.petcoms.fragments.mypage.MyPageFragment"
            android:label="fragment_mypage"
            tools:layout="@layout/fragment_mypage" />
    <fragment
            android:id="@+id/recordFragment"
            android:name="kevin.android.petcoms.fragments.record.RecordFragment"
            android:label="fragment_record"
            tools:layout="@layout/fragment_record" />
    <fragment
            android:id="@+id/statisticsFragment"
            android:name="kevin.android.petcoms.fragments.statistics.StatisticsFragment"
            android:label="fragment_statistics"
            tools:layout="@layout/fragment_statistics" />
</navigation>